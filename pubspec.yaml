name: feed_grinder
description: "Gilingang Pangkain para sa mga Hayop - Animal Feed Grinder App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive_flutter: ^1.1.0
  provider: ^6.1.2
  go_router: ^13.2.0
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.1
  uuid: ^4.5.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_native_splash: ^2.4.0

flutter:
  uses-material-design: true
