import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic test placeholder - the app requires Hive initialization
    expect(true, isTrue);
  });
}
